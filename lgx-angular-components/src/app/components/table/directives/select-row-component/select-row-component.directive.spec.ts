import { SelectRowComponentDirective } from './select-row-component.directive';

describe('SelectRowComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectRowComponentDirective(null);
    expect(directive).toBeTruthy();
  });
});
